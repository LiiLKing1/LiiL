#pragma once
#include <string>

namespace liil {
namespace command {

class AutomationEngine {
public:
    static void ExecuteMode(const std::string& modeName);
};

} // namespace command
} // namespace liil
